import {Route, Switch} from "react-router-dom";
import React from "react";

export default (
	<Switch>
		<Route path='/'/>
		<Route path='/about'/>
		<Route path='/work'/>
		<Route path='/contact'/>
		<Route/>
	</Switch>
)
